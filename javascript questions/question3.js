//Q.3 Create a function to calculate the area of a rectangle given its width and height.

let area =(width, height) => {
    return width*height;
}
let Area = area(10,5);
console.log(`the area of rectangle is ${Area}`);